# Pre-commit hooks configuration
# Runs 'make check' before every commit

repos:
  - repo: local
    hooks:
      - id: make-check
        name: Run make check
        entry: make check
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]